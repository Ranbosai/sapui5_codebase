# Shared Contracts

TypeScript interfaces shared across AuroraStream services to keep API contracts consistent.

## Usage

Install dependencies and build the package from the repository root:

```bash
npm install
npm run build --workspace @aurorastream/contracts
```

Import the generated types into frontend or backend services to guarantee compatibility between chat, upload, and metadata APIs.
